# deploy/env.frontend.production.example
#
# VPS上の frontend/.env.local のひな形。
# NEXT_PUBLIC_で始まる値はNext.jsのビルド時にコードへ埋め込まれるため、
# 「npm run build」より前にこのファイルを配置しておく必要がある。
# いずれも今のローカルの frontend/.env.local の値をそのままコピーする。

NEXT_PUBLIC_LINE_BASIC_ID="(ローカルの値をコピー)"
NEXT_PUBLIC_LIFF_ID="(ローカルの値をコピー)"
